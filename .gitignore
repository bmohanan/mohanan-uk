# macOS junk
.DS_Store
.DS_Store?
._*
.AppleDouble
.LSOverride
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~
*.sublime-*

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment / secrets
.env
.env.local
.env.*.local

# Node (in case we ever add tooling that uses it)
node_modules/

# Cloudflare Wrangler state — should NEVER be committed
.wrangler/
.dev.vars

# Claude Code per-project config — track permissions allowlist (shared),
# but ignore the local preview server config (per-machine).
.claude/launch.json
.claude/settings.local.json

# Lychee link-checker cache — regenerated on every run, no need to commit
.lycheecache

# Build artifacts
dist/
build/

# OG card source HTML — used locally to re-render og-image.png via
# headless Chrome; not meant to be served publicly. The rendered
# PNG (og-image.png) is what gets deployed.
og-image.html
